<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Di Gala" %></title>
	<%= include_gon(:init=>true) %>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= javascript_include_tag "application" %>
  </head>
  <body class="devise">
	<div id="container-default">
     <nav class="navbar navbar-default">
        	 <!-- Brand and toggle get grouped for better mobile display -->
             <div class="navbar-header">
                 <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-2">
                     <span class="sr-only">Toggle navigation</span>
                     <span class="icon-bar"></span>
                     <span class="icon-bar"></span>
                     <span class="icon-bar"></span>
                 </button>
                 <%= link_to "Di Gala - admin", root_path, class: "navbar-brand" %>
             </div>
			 <div class="collapse navbar-collapse col-sm-6 col-sm-offset-6 col-xs-12" id="navbar-collapse-2">
				 <ul class="nav navbar-nav">
				 	<%= render "devise/shared/links" %>
				</ul>
			</div>
	  </nav>
	</div>
 
    <div class="container-fluid">
        <div class="row devise-container">
            <%= yield %>
        </div><!-- row -->
		<div class="row">
      <footer>
        <p>&copy; Di Gala 2014</p>
      </footer>
  </div>


    </div> <!-- /container -->
<% # debug(params) if Rails.env.development? %>
  </body>
</html>

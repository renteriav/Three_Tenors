<%= form_for(@sound_clip, html: { multipart: true }) do |f| %>
  <% if @sound_clip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sound_clip.errors.count, "error") %> prohibited this sound_clip from being saved:</h2>

      <ul>
      <% @sound_clip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :attachment %>
    <%= f.file_field :attachment %>
  </div></br>
  <div class="actions">
    <%= f.submit "save", class: "btn btn-primary" %>
  </div>
<% end %>
